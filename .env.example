## Flask / Environment Configuration
# Primary environment selector (overrides FLASK_ENV). Values: development | testing | production
APP_ENV=production
FLASK_ENV=production
DEBUG=False
SECRET_KEY=your-secret-key-here
WTF_CSRF_SECRET_KEY=your-csrf-secret-key-here

## Database Configuration
# Replace with your production Postgres URL (Railway/Render/Neon etc.)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: enable SQL echo for debugging (never enable in real prod)
SQL_ECHO=False

# Email Configuration (optional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

## Stripe Billing (replace with live keys in production)
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_MONTHLY_PRICE_ID=price_monthly_live_xxx
STRIPE_YEARLY_PRICE_ID=price_yearly_live_xxx
STRIPE_LIFETIME_PRICE_ID=price_lifetime_live_xxx

## External API Keys (optional - set only the ones you use)
OPENAI_API_KEY=your-openai-api-key
FMP_API_KEY=your-fmp-api-key
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key
FINNHUB_API_KEY=your-finnhub-key
POLYGON_API_KEY=your-polygon-key
YAHOO_FINANCE_API_KEY=your-yahoo-key
NEWS_API_KEY=your-news-api-key

## Caching / Rate Limiting
REDIS_URL=redis://localhost:6379/0

## Server Settings
PORT=5000
# SERVER_NAME=aksjeradar.trade
PREFERRED_URL_SCHEME=https

## Mail / Notifications (Optional - for password reset etc.)
MAIL_DEFAULT_SENDER=your-email@gmail.com

## Deployment Notes
# 1. Copy this file to .env and fill in real secrets.
# 2. NEVER commit real secrets.
# 3. Set APP_ENV=production on your hosting provider.
# 4. Make sure DATABASE_URL uses postgresql:// (not postgres://).
